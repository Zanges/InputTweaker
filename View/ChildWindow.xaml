<Window x:Class="InputTweaker.View.ChildWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="ChildWindowView" Height="320" Width="400">
    <Window.DataContext>
        <Binding Path="Child" Source="{StaticResource Locator}" />
    </Window.DataContext>
    <Grid>
        <TextBlock Text="{Binding MessageFromParent}" HorizontalAlignment="Center" VerticalAlignment="Center" />
    </Grid>
</Window>
